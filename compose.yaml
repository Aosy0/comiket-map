services:
  comiketmap:
    image: nginx:alpine
    container_name: comiketmap
    restart: unless-stopped
    volumes:
      - ./app:/usr/share/nginx/html:ro
      - ./nginx/nginx.conf:/etc/nginx/nginx.conf:ro
    networks:
      - shared-network

networks:
  shared-network:
    external: true
